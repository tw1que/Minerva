# Agent Notes

- Base branch: dev
- Install deps: python -m pip install -r requirements-dev.txt
- Run tests: pytest
- bcrypt pinned to 4.0.1 for passlib compatibility on Windows
- Keep the current dev HTML interface updated alongside backend changes to reflect and validate usability.
- Plan to migrate the UI to a framework like React in the future.
